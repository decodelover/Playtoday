[project]
name = "playtoday-settlement-worker"
version = "0.0.0"
description = "Non-operational placeholder for the future PlayToday settlement worker."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.playtoday]
status = "placeholder"
