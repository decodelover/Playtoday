[project]
name = "playtoday-ingestion-worker"
version = "0.0.0"
description = "Identity and repository checks for the operational PlayToday ingestion worker."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.playtoday]
status = "operational"
runtime = "nodejs"
